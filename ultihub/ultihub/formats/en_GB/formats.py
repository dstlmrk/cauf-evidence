# Default output formats for the whole app (LANGUAGE_CODE = "en-gb").
# Used whenever a template renders a value with `|date` / `|time` without an
# explicit format argument. Templates that pass an explicit format keep it.
DATE_FORMAT = "Y/m/d"
DATETIME_FORMAT = "Y/m/d H:i"
