services:
  test_db:
    image: postgres:16.4-alpine
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ultihub
      POSTGRES_DB: ultihub
      POSTGRES_PASSWORD: password
